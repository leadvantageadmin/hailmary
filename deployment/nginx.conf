server {
    listen 80;
    server_name hailmary.leadvantageglobal.com;

    # Redirect HTTP to HTTPS (optional, for future SSL setup)
    # return 301 https://$server_name$request_uri;

    # For now, serve HTTP directly
    location / {
        proxy_pass http://web:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# Future HTTPS configuration (uncomment when you have SSL certificate)
# server {
#     listen 443 ssl;
#     server_name hailmary.leadvantageglobal.com;
#     
#     ssl_certificate /etc/ssl/certs/hailmary.leadvantageglobal.com.crt;
#     ssl_certificate_key /etc/ssl/private/hailmary.leadvantageglobal.com.key;
#     
#     location / {
#         proxy_pass http://web:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
